#include "string.hpp"

int main() {
	/*
	String a;

  String b('z');

  std::cout << "string a cap: " << a.capacity() << std::endl;
  std::cout << "string b cap: " << b.capacity() << std::endl;

  std::cout << "string a len: " << a.length() << std::endl;
  std::cout << "string b len: " << b.length() << std::endl;

  String c("abc");
  const String d("xyz");

  std::cout << "str c index 1: " << c[1] << std::endl;
  std::cout << "str d index 1: " << d[1] << std::endl;

  String e = c + d;
  std::cout << "string c len: " << c.length() << std::endl;
  std::cout << "string d len: " << d.length() << std::endl;
  std::cout << "string e len: " << e.length() << std::endl;
  //std::cout << "string c+d: " <<(c+d) << std::endl;

 // print string e
  std::cout << "string e: ";
  for (int i=0; i < e.length(); ++i) {
	  std::cout << e[i];
  }
  std::cout << std::endl;

  std::cout << "c == d: " << (c == d) << std::endl;
  std::cout << "c == c: " << (c == c) << std::endl;

  std::cout << "c < d: " << (c < d) << std::endl;
  std::cout << "c > d: " << (d < c) << std::endl;

  String f("accept");
  String g("access");
  std::cout << "accept < access: " << (f < g )<< std::endl;

  String h("acceptance");
  std::cout << "accept < acceptance: " << (f < h) << std::endl;

  std::cout << "access < acceptance: " << (g < h) << std::endl;

  f = "alligator";
  g = "b";
  std::cout << "alligator < b: " << (f < g) << std::endl;

  g = "alligator";
  std::cout << "alligator < alligator: " << (f < g) << std::endl;


  String i("ABCDEFG");

  std::cout << "i.substr(0,2): " << i.substr(0, 2) << std::endl;
  std::cout << "i.substr(3,5): " << i.substr(3, 5) << std::endl;
  std::cout << "i.substr(1,1): " << i.substr(1, 1) << std::endl;

  String j("abcabc");
  std::cout << "j.findstr(0,'abc'): 0 == " << j.findstr(0, "abc") << std::endl;
  std::cout << "j.findstr(1,'abc'): 3 == " << j.findstr(1, "abc") << std::endl;
  std::cout << "j.findstr(4,'abc'): -1 == " << j.findstr(4, "abc") << std::endl;

  String k("abcabc");
  std::cout << "k.findchar(0,'a'): 0 == " << k.findchar(0, 'a') << std::endl;
  std::cout << "k.findchar(1,'a'): 3 == " << k.findchar(1, 'a') << std::endl;
 

	char a[] = {'a', 'b', 'c', '\0'};
	String m("a");

	bool b = (a == m);
  */

  }


